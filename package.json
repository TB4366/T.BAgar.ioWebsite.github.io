{
  "name": "tb-chat-proxy",
  "version": "1.0.0",
  "description": "Server proxy for T.B Chat Bot (keeps API key server-side and enforces no-code rules)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "dotenv": "^16.1.4"
  }
}
